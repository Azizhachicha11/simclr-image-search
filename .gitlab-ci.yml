stages:
  - test

verify-deps:
  stage: test
  image: python:3.12-slim
  script:
<<<<<<< HEAD

=======
>>>>>>> e7c63b8 (ci: add model download script)
    - pip install --upgrade pip
    - pip install fastapi==0.110.0 uvicorn==0.27.0 pytest==8.0.0 httpx==0.27.0 huggingface-hub==0.21.0 Pillow==10.3.0 numpy==1.26.4
    - python -c 'import fastapi; print("fastapi ok")'
    - python -c 'import numpy; print("numpy ok")'
    - echo "All lightweight dependencies installed!"

run-tests:
  stage: test
  image: python:3.12-slim
  before_script:
    - pip install --upgrade pip
    - pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cpu
    - pip install faiss-cpu==1.8.0 fastapi==0.110.0 uvicorn==0.27.0 pytest==8.0.0 httpx==0.27.0 huggingface-hub==0.21.0 Pillow==10.3.0 numpy==1.26.4 python-multipart==0.0.9
    - mkdir -p checkpoints index
    - python download_model.py
  script:
    - pytest tests/ -v --tb=short --ignore=tests/test_model.py

    - python --version
    - python -c "import torch; print('torch:', torch.__version__)"
    - python -c "import faiss; print('faiss: ok')"
    - echo "All dependencies installed successfully!"

